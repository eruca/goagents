# Skills Example

This example wires a skill provider and a read-only tool into the Agent.

The skill is model-facing guidance. The tool is the executable action.

Run it with:

```bash
go run ./examples/skills
```
