# Prompt Example

This example records the first model request from a deterministic Agent run.

Prompt blocks are model-facing instructions. The example wires one static prompt block and one module that provides a system prompt block and a cacheable skill. It prints compact message order and label checks instead of the full prompt text.

Run it with:

```bash
go run ./examples/prompt
```
