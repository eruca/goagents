# GoAgents 进程内模块 `v0.1.2` 发布记录

日期：2026-08-12

状态：两个不可变 module tags 已发布并完成公开代理交叉验证。

## 发布身份

- 候选 commit：`e036a14289c5009be767033c6ae6054953b68018`。
- `goagent/v0.1.2` tag object：`6ad9fb07a5f3531486901fe99549e21f834fa185`，peeled SHA 为候选 commit。
- `llmkit/v0.1.2` tag object：`569bf652a3fbb819b50f46711a72b492aad6d8d2`，peeled SHA 为候选 commit。
- 使用一次 `git push --atomic` 推送候选分支和两个新 annotated tags，exit 0。
- 旧 `goagent/v0.1.1`、`llmkit/v0.1.1` tag object 与 peeled SHA 均保持不变。

## 公开消费证据

全新且彼此隔离的 module cache 分别使用 `proxy.golang.org` 和 `goproxy.cn` 下载两个
`v0.1.2`，均 exit 0，结果一致：

| Module | module checksum | go.mod checksum |
|---|---|---|
| `goagent v0.1.2` | `h1:MWRoRHtldmo0ausIWAOIIcYrS1Z6FqSOSkx+1e3S50A=` | `h1:tfzdmBBu3oE+Q7akFMVgJPXJTfx/1GykZo5jam28MBY=` |
| `llmkit v0.1.2` | `h1:+OSR9yw3bNVwRpEqlWutManB1KHgcsCrQOttnnZBQqM=` | `h1:H2YiuF4KoxJdtXfxOPmpsQzYbyr0uthIwHujb2QqHL4=` |

`sum.golang.org` 返回相同 checksums；官方 Proxy 的 Origin 对两个模块都绑定候选 commit。
AI Todo 在其默认 `goproxy.cn,direct` 路径下已成功执行 `go mod tidy` 和定向测试，原
`v0.1.1` checksum mismatch 不再复现。

## 边界

- 本次没有 GitHub Release、Host、OCI、registry 或生产部署。
- 没有真实 Provider、付费或业务出站。
- tags 不包含本发布后证据提交；该提交不得移动既有 tags。
