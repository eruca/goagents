# GoAgents 进程内模块 `v0.1.2` Tag 不可变性恢复

日期：2026-08-12

状态：本地候选与完整门禁已通过，尚未创建或推送 `v0.1.2` tags。

## 原因

- AI Todo Week 27 在默认 `GOPROXY=https://goproxy.cn,direct` 下下载
  `goagent@v0.1.1` 时触发 checksum mismatch。
- `sum.golang.org` 与 `proxy.golang.org` 返回的 checksum 与 AI Todo `go.sum` 一致；
  `goproxy.cn` 保留了 2026-08-07 的另一份 `v0.1.1` 内容。
- 当前远端 `goagent/v0.1.1`、`llmkit/v0.1.1` 均 peel 到 2026-08-08 的
  `1fc08774f4ed26ba9abbf867cd671680c8fd2ef5`。同一 tag 已被不同公开代理冻结为不同内容，
  不能再通过移动、删除或覆盖旧 tag 修复。

## 恢复范围

- 只发布 `goagent/v0.1.2` 与 `llmkit/v0.1.2`；旧 tags 永不再移动。
- `llmkit v0.1.2` 精确依赖 `goagent v0.1.2`，发布模块不加入本地 `replace`。
- 不修改 Go 公共 API 或 runtime 行为，不发布 Host、进程协议、OCI 或 Release。
- 使用既有 deterministic file proxy、空缓存 consumer、release layout、test/race/vet 和
  workspace 门禁验证同一候选。

## 发布门禁

- 新 tags 创建前必须证明候选 Diff 只有版本元数据、门禁入口和本记录。
- 两个 tags 必须是新的 annotated tags，且不得触碰 `v0.1.1` tag 对象。
- 推送后必须从公开代理的全新缓存下载，校验 module 版本、checksum、LICENSE 和无 Replace。
- 若任一公开代理尚未收敛或返回不同内容，保持阻塞，不让 AI Todo 把该版本计为可复现依赖。

## 本地冻结证据

- `goagent` 与 `llmkit` 分别通过 `GOWORK=off` 的 tidy diff、test、race、vet，exit 0。
- candidate proxy 正向与许可证/路径/cache/不存在版本负面场景通过，仓外 consumer 精确解析
  两个 `v0.1.2` 且 Replace 为空，exit 0。
- release layout 正向与负面门禁、`git diff --check` 均 exit 0。
- `scripts/verify-all.sh` exit 0，最终输出 `goagents workspace verification passed`；真实 Provider
  未配置并保持 skip，没有业务出站。
- 当前候选 Diff 审查：0 Critical、0 Important、0 Minor；未修改 runtime/API/Host 行为。
